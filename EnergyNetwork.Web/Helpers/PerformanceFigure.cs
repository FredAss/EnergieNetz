namespace EnergyNetwork.Web.Helpers
{
  public static class PerformanceFigure
  {
    public static double Calculate(double denominator, double numerator)
    {
      return (denominator / numerator);
    }
  }
}